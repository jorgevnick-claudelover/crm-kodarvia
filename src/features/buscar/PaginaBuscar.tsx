import { EnConstruccion } from "@/components/comunes/EnConstruccion"

/** Provisional: la sustituye el módulo de buscar en la fase 2. */
export function PaginaBuscar() {
  return <EnConstruccion titulo="Buscar" />
}

export default PaginaBuscar
