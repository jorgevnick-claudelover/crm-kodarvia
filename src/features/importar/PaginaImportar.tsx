import { EnConstruccion } from "@/components/comunes/EnConstruccion"

/** Provisional: la sustituye el módulo de importar en la fase 2. */
export function PaginaImportar() {
  return <EnConstruccion titulo="Importar" />
}

export default PaginaImportar
