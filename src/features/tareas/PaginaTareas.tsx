import { EnConstruccion } from "@/components/comunes/EnConstruccion"

/** Provisional: la sustituye el módulo de tareas en la fase 2. */
export function PaginaTareas() {
  return <EnConstruccion titulo="Tareas" />
}

export default PaginaTareas
