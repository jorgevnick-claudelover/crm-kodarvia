import { EnConstruccion } from "@/components/comunes/EnConstruccion"

/** Provisional: la sustituye el módulo de contactos en la fase 2. */
export function PaginaContacto() {
  return <EnConstruccion titulo="Contacto" />
}

export default PaginaContacto
