import { EnConstruccion } from "@/components/comunes/EnConstruccion"

/** Provisional: la sustituye el módulo de hoy en la fase 2. */
export function PaginaHoy() {
  return <EnConstruccion titulo="Hoy" />
}

export default PaginaHoy
