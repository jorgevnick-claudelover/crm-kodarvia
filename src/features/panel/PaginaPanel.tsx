import { EnConstruccion } from "@/components/comunes/EnConstruccion"

/** Provisional: la sustituye el módulo de panel en la fase 2. */
export function PaginaPanel() {
  return <EnConstruccion titulo="Panel" />
}

export default PaginaPanel
